container_commands:
  01_permissions:
    command: |
      set -e
      cd /var/app/current
      chown -R webapp:webapp storage bootstrap/cache || true
      chmod -R 775 storage bootstrap/cache || true
    leader_only: true
